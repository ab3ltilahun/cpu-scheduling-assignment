# cpu-scheduling-assignment
Round Robin scheduling
It is one of the oldest and most widely used algorithms.It allocate the CPU for one Quantum time Q 
to each process in the ready queue.If the process has blocked or finished before the quantum has 
elapsed, the CPU switching is done when the process block.This scheme is repeated until all 
processes are finished.

For More Visit Documentation.
